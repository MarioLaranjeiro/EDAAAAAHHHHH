#include <stdio.h>

void maiorvalorarray(int tab[],int n)
{ 
     int maior;
      if(maior<*tab && *tab<=tab[n-1])
    {
     maior=*tab;
    maiorvalorarray(tab+1,n);
    }
    
    else
   printf("o maior valor é %d",maior);

}

int main() 
{  
    int n;
    int array[n];
    
    printf("Insira o nr de elementos: ");
    scanf("%d",&n);
    
    for(int i=0;i<n;i++)
    {
    scanf("%d",&array[i]);
    }
    maiorvalorarray(&array[0],n);
}